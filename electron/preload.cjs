// Preload runs with context isolation on. The mockup is a plain web app that
// needs no privileged bridge, so we expose nothing — this file exists to keep a
// clean, isolated boundary between the renderer and Node.
