# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# This project uses bun (bun.lock); ignore npm's lockfile to avoid mixing package managers
package-lock.json

# Local env / secrets (e.g. SHALION_API_TOKEN for the live-data proxy) — never commit
.env
.env.*
!.env.example
# Vercel build output / local project link
.vercel
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
